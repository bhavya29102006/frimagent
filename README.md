# FirmAgent

AI agent for autonomous embedded firmware testing (Black Box Hackathon 2026, PS3).
Reads firmware, decides what to test, runs tests in the Wokwi simulator, detects failures and writes a report.

Docs: see `docs/` (PRD, TRD, App Flow, UI/UX, Schema, Implementation Plan, Roadmap, Rules).
Start: see `START_HERE.md`.
